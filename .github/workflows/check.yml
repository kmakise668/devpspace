name: Deploy to Hosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Deploy to Hosting
      run: |
        node -e "const FtpDeploy = require('ftp-deploy'); const ftpDeploy = new FtpDeploy(); ftpDeploy.deploy({ server: '194.39.65.17', username: 'devpspa1', password: '~@KnCwYddcpro547', localRoot: './index.php', remoteRoot: '/httpdocs' })"

